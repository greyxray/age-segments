# Prepare clean python env

```bash
pyenv install 3.8.3
pyenv virtualenv 3.8.3 myenv
pyenv local myenv
```

# Install

```bash

# git clone this repo

pip install -r requirements.txt
pip install -e .

```
